/**
 @toc
 1. setup - whitelist, appPath, html5Mode
 */

'use strict';

angular.module('myApp', ['ct.ui.router.extras'])
    .config(['$stickyStateProvider', '$stateProvider','$urlRouterProvider',
      function ($stickyStateProvider, $stateProvider, $urlRouterProvider) {
        // TODO: stuff here
    }]);